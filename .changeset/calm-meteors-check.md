---
"@lgrammel/open-meteo-tool": patch
---

Fix AI SDK `toolsContext` typing for the weather tool by making `temperatureUnit` the required context key, and add Vitest type tests for `ToolLoopAgent` context inference.
