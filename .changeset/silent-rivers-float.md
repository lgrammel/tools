---
"@lgrammel/open-meteo-tool": minor
---

Remove the bundled `units` weather context setting and expose Open-Meteo's `temperatureUnit`, `windSpeedUnit`, and `precipitationUnit` settings individually.
